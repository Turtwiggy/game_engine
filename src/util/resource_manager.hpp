#pragma once

#include <string>
#include <filesystem>

class ResourceManager
{
public:
    //std::string get_current_path()
    //{
    //    const std::string dir = std::string(std::filesystem::current_path().generic_u8string());
    //    printf("your path: %s", dir);
    //    return dir;
    //}
};
